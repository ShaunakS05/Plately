import React from "react";
import { Routes, Route, Link } from "react-router-dom";
import Dashboard from "./Dashboard"; // Import Dashboard
import "./index.css"; // Ensure you have a CSS file if needed

function App() {
  
  return (
    <div>
      {/* Hero Section */}
      <section>
        <h1>Plately</h1>
        <h2>Your AI Menu Engineer</h2>
        <p>Optimize your restaurant’s menu using AI-driven insights.</p>

        {/* Use Link instead of <a href> */}

      </section>

      {/* React Router */}
      
    </div>
  );
}

export default App;
